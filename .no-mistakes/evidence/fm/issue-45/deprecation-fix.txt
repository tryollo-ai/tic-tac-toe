Verified: agent-dispatch.yml — app-id → client-id rename
==========================================================

Commit: 73978bb41ade6b4cd27bc9325493f607c7bec883
File:   .github/workflows/agent-dispatch.yml

Two steps updated (both now use `client-id`):

  Line 64-70: "Mint Projects token" step
    BEFORE:  app-id: ${{ secrets.PROJECTS_APP_ID }}
    AFTER:   client-id: ${{ secrets.PROJECTS_APP_ID }}

  Line 193-199: "Mint app token" step
    BEFORE:  app-id: ${{ secrets.PROJECTS_APP_ID }}
    AFTER:   client-id: ${{ secrets.PROJECTS_APP_ID }}

Scope check — no residual `app-id` in any .github/workflows/ file:
  $ grep -rn "app-id" .github/     →  (no output, zero matches)

All `create-github-app-token@v3` usages in the repo:
  .github/workflows/agent-dispatch.yml:66   (Mint Projects token)
  .github/workflows/agent-dispatch.yml:195  (Mint app token)
  — both updated, none missed.

Secret unchanged: PROJECTS_APP_ID still supplies the numeric GitHub App ID.
The action accepts this value under either key name; no secret rotation needed.
