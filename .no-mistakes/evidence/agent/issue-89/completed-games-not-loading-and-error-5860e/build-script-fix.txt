## Build script change (package.json)

BEFORE (base commit 07d4fde):
  "build": "next build"

AFTER (target commit ff1aeec):
  "build": "[ \"$VERCEL_ENV\" = \"production\" ] && prisma migrate deploy; next build"

Shell semantics:
  - When VERCEL_ENV=production  → runs `prisma migrate deploy` then `next build`
  - When VERCEL_ENV≠production  → skips migrate (test exits 1), but `;` ensures `next build` still runs
  - `prisma migrate deploy` is non-interactive and idempotent: already-applied migrations are skipped

This ensures every Vercel production deploy automatically applies the committed
`20260626000000_completed_game_participants` migration (which adds `player_x` / `player_o`
to the `completed_games` table) before building — fixing the P2022 runtime error.
